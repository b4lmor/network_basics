buildscript {
    ext {
        repos = {
            mavenCentral()
        }
    }
    repositories repos
}

configure(allprojects) {
    repositories repos
    buildscript {
        repositories repos
    }
}